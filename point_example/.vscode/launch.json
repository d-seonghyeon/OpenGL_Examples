{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB (CMake target)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/point_example",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "MIMode": "lldb"
        }
    ]
}
